import { HeroContainer, Container, HeroTitle } from './Hero.module'

const Hero = () => (
  <HeroContainer>
    <Container>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </Container>
  </HeroContainer>
)

export default Hero
